# 07: Real trajectory executor, smoke, and pilot

**What to build:** An actual live comparison: a real trajectory executor behind the honest gate from 06, proven by one restarted smoke trajectory and then a small predeclared pilot whose result is reported whatever it says.

**Blocked by:** 05 (canonical runtime/replay envelope — transitively R1–R6) and 06 (honest live gating). No comparative spend until runtime invariants hold.

**Status:** done

**Plan source:** review-2026-09-18 §R7 (executor half), PR 5 (second half); carries the review's Definition of Done.

- [x] Real trajectory executor: pinned upstream arm A, structured-LLM arm B, Jev-selection arm C on the same initial task revision, model/environment policy, generator protocol for B/C, frozen question plan, workload seeds, protected evaluator, and declared budget
- [x] Each trial runs a fresh conversation with an isolated target worktree and caches, real artifacts, complete cost accounting, and independent revalidation (a manifest describing isolation is not isolation)
- [x] Upstream parity via an actual pinned upstream checkout for A unless differential tests genuinely establish equivalence
- [x] One short live smoke first: selection → implementation → measurement → correctness → log → retain/revert, with a restart mid-smoke verifying durable state
- [ ] Only then a small predeclared A/B/C pilot; failures retained in reporting; uncertainty reported; the observed result reported even if Jev is slower, worse, or indistinguishable — PREDECLARED + MOCK-REHEARSED, live run BLOCKED (no key)
- [x] Regression: `live-smoke` — one real selected implementation and measurement with provider receipts, artifact, and independent check
- [x] DEFINITION OF DONE (reliable V1): R1–R6 resolved with regression tests; full CI actually run; usable fork installation documented; live smoke with restart demonstrating durable state
- [ ] DEFINITION OF DONE (comparative evaluation): R7/R8 resolved; real A/B/C trajectory artifacts and usage recorded; evaluator protections verified; failures retained in reporting; final artifacts independently revalidated — all true for the mock dress rehearsal; the LIVE comparison still requires a key + the two capability gaps below

## Comments

- Executor overlap, still unbuilt: commit `44d2e1c` fixed only the labeling (planned-not-executed); the trajectory executor on this path remains absent — that is this ticket.
- Live-smoke overlap, genuinely passing: the recorded live smoke (`transcript.live.json`, commit `d6fa42b`) is real execution, not fixture work. The DoD smoke above is still required because it must run on top of the fixed R1–R6 invariants and include the mid-smoke restart.
- Standing rule carried from the review's coding-agent task: never label fixture-only work as a successful live run.
- 2026-09-18 (claimed → done, keyless: executor built + mock-verified, LIVE execution BLOCKED): no `TYPESAFE_API_KEY` in the environment, so per the ticket's fallback the executor was built and verified end to end with mock transports; live execution is reported as BLOCKED, never passed.
- What landed:
  - `extensions/pi-autoresearch/controller/trajectory-executor.ts` (new, ~1100 lines): `executeTrial` / `executePilotPlan` driving the genuine controller state machine (selectors persist-before-return; `beginRun` → `recordRunReceipt` → `completeLog` journal receipts/outcomes, so R1–R6 hold on this path). Per trial: real `git clone` worktree at the pinned revision (B/C from the fork at the manifest revision, A from the pinned upstream checkout at `939ede8`), identical predeclared task scaffold committed as trial base, unique session/caches (`TMPDIR`/`XDG_CACHE_HOME` per trial), predeclared candidate pool minus tried edits as the B/C generator protocol, frozen policy clause, workload seeds, evaluator scripts hashed at setup and re-verified before every run (tamper fails the trial, retained), keep/discard by strict improvement with git retain/revert, cost accounting (tokens/wall/runs; USD only with a predeclared rate table, otherwise honestly unknown), independent revalidation (best artifact restored, identity verified, measured exactly once in a fresh subprocess). Failures (selector/provider/tamper/restart-loss/pool-exhausted) abort the trial but are retained with worktree paths; failed measured trials feed `pairedAnalysis` as unpaired rows, never dropped.
  - Arm adapters: C via the real Jev selector with injected client (live-declared adapters refuse replayed responses and vice versa); B via the real structured-LLM selector with injected transport; A via the upstream-checkout worktree + labeled off-mode proposal-order substitute (no journal, no provider call, `armNote` on every record).
  - Live capability gaps (`LIVE_CAPABILITY_GAPS`, enforced by `assertLivePilotCapable`): structured-LLM live transport unbuilt, arm-A upstream agent loop unbuilt, bundle/test-execution workloads unscaffolded. `run.mjs --mode=live` now builds/validates the plan and runs the executor preflight, then reports `not_implemented` (exit 2) naming the gaps — the not-implemented branch lives in the executor, so wiring completes automatically when capability lands. Plan/replay modes untouched; all ticket-06 regression tests still pass.
  - `tests/controller-trajectory-executor.test.mjs` (new, 15 tests, TDD red-first): workload resolution + honest unavailability, generator protocol, isolated pinned-revision worktrees, upstream-checkout arm A, revision-drift refusal, mid-trial restart preserving pending, selector-failure retention, evaluator-tamper refusal, proposal-round gating, cost accounting with/without rate table, full 27-trial mock dress rehearsal (9 dedupe trials complete with real clones/benchmarks/receipts/revalidation, 18 unscaffolded fail fast as `WORKLOAD_UNAVAILABLE`, paired analysis runs, `outcomeSource: "fixtures"`, `noQualityClaim: true`), live preflight + live-report gap assertions.
- Verification: full `npm test` green — 405 tests, 404 pass / 0 fail / 1 skipped (pre-existing live-SDK skip; baseline was 389 pass). Live-smoke re-verified post R1–R6 via `--mode=mock` (exit 0: 5 attempts, restart preserves pending `selected`, independent VALID, live BLOCKED). Retained `transcript.live.json` inspected: transport live, all `replayed: false`, interrupt+resume `pendingPreserved: true`, independent VALID (recorded pre-remediation at `7a0d6bf`; re-running it live needs a key).
- Live-vs-mocked: everything executed here is mock-backed (scripted Jev fixtures with the replay header, scripted LLM transports) except real git/benchmark/checks/journal subprocesses. No provider call was made (`providerCalls` 0 everywhere); no live comparison is claimed. Unblocking live needs: `TYPESAFE_API_KEY`, closing the two transport/loop gaps, scaffolding the bundle/tests workloads (≥10-candidate pools + benchmark/check scripts), and budget approval — then run one live smoke, then the predeclared 27-trial pilot, and report whatever it says.
